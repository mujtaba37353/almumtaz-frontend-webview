# Web on same PC (prefer 127.0.0.1 — avoids IPv6 localhost issues on Windows):
API_URL=http://127.0.0.1:5000/api
# Phone / another device on Wi-Fi (use your PC IPv4 from ipconfig):
# API_URL=http://192.168.x.x:5000/api
